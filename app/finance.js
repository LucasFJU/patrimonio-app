'use client';
import {useMemo,useRef,useState} from 'react';
import {ArrowDownLeft,ArrowUpRight,CalendarDays,Check,CreditCard,Download,Plus,ReceiptText,Repeat,Trash2,Upload,Wallet} from 'lucide-react';
import {DEFAULT_FINANCE,FINANCE_CATEGORIES,accountBalances,addMonthlyRecurring,csvFingerprint,importPreviewRows,installmentSchedule,invoiceSummaries,parseStatementCsv,summarizeFinance} from '../lib/finance.mjs';
import {money,dateISO,moneyWeightedReturn,periodStartDate} from '../lib/portfolio.mjs';

const todayMonth=()=>dateISO().slice(0,7);
const fmtDate=d=>new Date(`${d}T12:00:00`).toLocaleDateString('pt-BR');
const empty=(f)=>f||DEFAULT_FINANCE();
function Card({title,children,icon:Icon=Wallet}){return <article className="panel finance-card"><div className="panel-heading"><h2>{title}</h2><Icon size={19}/></div>{children}</article>}
function Stats({summary,display}){return <section className="finance-stats"><article className="panel"><span>Saldo nas contas</span><strong>{display(summary.cash)}</strong></article><article className="panel"><span>Despesas do mês</span><strong>{display(summary.expenses)}</strong></article><article className="panel"><span>Faturas em aberto</span><strong>{display(summary.invoices.reduce((n,i)=>n+i.open,0))}</strong></article><article className="panel"><span>Fluxo líquido do mês</span><strong className={summary.netCashFlow<0?'negative':''}>{display(summary.netCashFlow)}</strong></article></section>}
export function FinanceDashboard({finance,month,display,onOpen}){const f=empty(finance),s=summarizeFinance(f,month);return <section className="finance-dashboard panel spaced"><div className="panel-heading"><div><h2>Seu mês financeiro</h2><p>Contas, despesas e faturas no mesmo lugar.</p></div><button className="text-button" onClick={onOpen}>Abrir controle financeiro <ArrowUpRight size={16}/></button></div><div className="finance-summary-grid"><div><span>Disponível nas contas</span><strong>{display(s.cash)}</strong></div><div><span>Despesas do mês (cartão incluído)</span><strong>{display(s.expenses)}</strong></div><div><span>Faturas a vencer neste mês</span><strong>{display(s.due)}</strong></div></div></section>}
export function Finance({state,onSave,display,notify}){
 const finance=empty(state.finance),month=todayMonth(),summary=summarizeFinance(finance,month),[type,setType]=useState('expense'),[csvRows,setCsvRows]=useState(null),[csvAccount,setCsvAccount]=useState(''),[csvCategory,setCsvCategory]=useState('Outros'),[budgetValues,setBudgetValues]=useState(finance.budgets[month]||{}),[showAccount,setShowAccount]=useState(false),[showCard,setShowCard]=useState(false),[busy,setBusy]=useState(false),[period,setPeriod]=useState('12'),[benchmark,setBenchmark]=useState(null),[benchmarkLoading,setBenchmarkLoading]=useState(false);
 const csvRef=useRef(null);const categories=useMemo(()=>[...new Set([...FINANCE_CATEGORIES,...(finance.categories||[])])],[finance.categories]);
 const commit=async(next,message,events=state.events)=>{setBusy(true);try{return await onSave({...state,events,finance:next},message)!==false;}finally{setBusy(false);}};
 const addAccount=async e=>{e.preventDefault();const form=e.currentTarget,d=new FormData(form);const next=structuredClone(finance);next.accounts.push({id:crypto.randomUUID(),name:String(d.get('name')).trim(),type:String(d.get('type')),openingBalance:Number(d.get('openingBalance')||0),openingDate:String(d.get('openingDate'))});if(await commit(next,'Conta adicionada.')){form.reset();setShowAccount(false);}};
 const addCard=async e=>{e.preventDefault();const form=e.currentTarget,d=new FormData(form);const next=structuredClone(finance);next.cards.push({id:crypto.randomUUID(),name:String(d.get('name')).trim(),accountId:String(d.get('accountId')),closeDay:Number(d.get('closeDay')),dueDay:Number(d.get('dueDay'))});if(await commit(next,'Cartão adicionado.')){form.reset();setShowCard(false);}};
 const addTransaction=async e=>{e.preventDefault();const form=e.currentTarget,d=new FormData(form);const kind=String(d.get('type')),amount=Number(d.get('amount')),date=String(d.get('date')),description=String(d.get('description')).trim(),category=String(d.get('category')||'Outros');const next=structuredClone(finance);let eventsNext=state.events;const tx={id:crypto.randomUUID(),type:kind,amount,date,description,category};if(kind==='income'||kind==='expense'){tx.accountId=String(d.get('accountId'));}
  if(kind==='transfer'){tx.accountId=String(d.get('accountId'));tx.toAccountId=String(d.get('toAccountId'));if(tx.accountId===tx.toAccountId)return notify('Escolha contas diferentes para a transferência.');}
  if(kind==='investment'){tx.accountId=String(d.get('accountId'));tx.assetId=String(d.get('assetId'));tx.type='investment';const event={id:crypto.randomUUID(),date,kind:'aporte',assetId:tx.assetId,amount,note:`Aporte via ${finance.accounts.find(a=>a.id===tx.accountId)?.name||'conta'}: ${description}`,createdAt:new Date().toISOString()};tx.portfolioEventId=event.id;eventsNext=[...state.events,event];}
  if(kind==='card_purchase'){tx.cardId=String(d.get('cardId'));tx.installments=Number(d.get('installments'));const card=finance.cards.find(c=>c.id===tx.cardId);tx.closeDay=card.closeDay;tx.dueDay=card.dueDay;tx.installmentParts=installmentSchedule({date,amount,installments:tx.installments,closeDay:card.closeDay,dueDay:card.dueDay});tx.invoiceMonth=tx.installmentParts[0].invoiceMonth;tx.dueDate=tx.installmentParts[0].dueDate;}
  if(kind==='card_payment'){tx.type=kind;const [cardId,invoiceMonth]=String(d.get('invoiceKey')).split('|');tx.cardId=cardId;tx.accountId=finance.cards.find(c=>c.id===tx.cardId)?.accountId;tx.invoiceMonth=invoiceMonth;const due=invoiceSummaries(finance).find(i=>i.cardId===tx.cardId&&i.invoiceMonth===tx.invoiceMonth);if(!due||amount>due.open+.005)return notify('O pagamento não pode ser maior que o saldo aberto dessa fatura.');}
  if(d.get('recurring')==='on'&&['income','expense'].includes(kind)){const recurringId=crypto.randomUUID();tx.recurringId=recurringId;next.recurring.push({id:recurringId,type:kind,accountId:tx.accountId,amount,description,category,day:Number(date.slice(8,10))});}
  next.transactions.push(tx);if(await commit(next,kind==='investment'?'Aporte registrado e vinculado à carteira.':'Lançamento financeiro salvo.',eventsNext))form.reset();};
 const deleteTransaction=async tx=>{if(!confirm('Excluir este lançamento?'))return;const next=structuredClone(finance);next.transactions=next.transactions.filter(t=>t.id!==tx.id);if(tx.recurringId&&!next.transactions.some(t=>t.recurringId===tx.recurringId))next.recurring=next.recurring.filter(r=>r.id!==tx.recurringId);if(tx.type==='investment'){const stateNext={...state,events:state.events.filter(e=>e.id!==tx.portfolioEventId),finance:next};await onSave(stateNext,'Lançamento e aporte removidos.');return;}await commit(next,'Lançamento removido.');};
 const loadCsv=async e=>{const file=e.target.files?.[0];e.target.value='';if(!file)return;try{const rows=parseStatementCsv(await file.text(),state.startDate);setCsvRows(importPreviewRows(rows,finance.transactions));setCsvAccount(finance.accounts[0]?.id||'');notify(`${rows.length} linhas encontradas. Revise antes de importar.`);}catch(err){notify(err.message);}};
const exportFinanceCsv=()=>{const labels={income:'Receita',expense:'Despesa',transfer:'Transferência',investment:'Aporte',card_purchase:'Compra no cartão',card_payment:'Pagamento de fatura'},escape=value=>`"${String(value??'').replace(/"/g,'""')}"`,rows=[['Data','Tipo','Descrição','Categoria','Valor (R$)','Conta/cartão','Fatura'],...finance.transactions.map(t=>[t.date,labels[t.type],t.description,t.category||'',t.amount.toFixed(2).replace('.',','),finance.accounts.find(a=>a.id===t.accountId)?.name||finance.cards.find(c=>c.id===t.cardId)?.name||'',t.invoiceMonth||''])];const blob=new Blob(['\uFEFF'+rows.map(r=>r.map(escape).join(';')).join('\r\n')],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`controle-financeiro-${dateISO()}.csv`;a.click();URL.revokeObjectURL(url);};
 const importCsv=async()=>{if(!csvAccount)return notify('Cadastre ou selecione uma conta antes da importação.');const selected=csvRows.filter(r=>r.selected&&!r.duplicate);if(!selected.length)return notify('Não há linhas novas selecionadas para importar.');const next=structuredClone(finance);next.transactions.push(...selected.map(r=>({id:crypto.randomUUID(),type:r.type,accountId:csvAccount,amount:r.amount,date:r.date,description:r.description,category:r.category||csvCategory,importFingerprint:csvFingerprint(r)})));if(await commit(next,`${selected.length} lançamentos importados.`))setCsvRows(null);};
 const saveBudget=async e=>{e.preventDefault();const next=structuredClone(finance);next.budgets[month]=Object.fromEntries(categories.map(c=>[c,Number(budgetValues[c]||0)]));await commit(next,'Orçamento do mês salvo.');};
 const addCategory=async e=>{e.preventDefault();const form=e.currentTarget,d=new FormData(form),name=String(d.get('category')).trim().slice(0,60);if(!name)return;const next=structuredClone(finance);next.categories=[...new Set([...(next.categories||[]),name])];if(await commit(next,'Categoria criada.'))form.reset();};
 const addRecurring=async()=>{const result=addMonthlyRecurring(finance,month);if(!result.added)return notify('As despesas recorrentes deste mês já foram lançadas.');await commit(result.finance,`${result.added} lançamento(s) recorrente(s) adicionados.`);};
 const runBenchmarks=async selected=>{setPeriod(selected);setBenchmarkLoading(true);try{const startDate=periodStartDate(state,selected);const response=await fetch(`/api/benchmarks?period=${selected}&startDate=${startDate}`);const data=await response.json();if(!response.ok)throw new Error(data.error||'Não foi possível carregar as referências.');setBenchmark(data);}catch(e){setBenchmark({error:e.message,results:[]});}finally{setBenchmarkLoading(false);}};
 const balances=accountBalances(finance),invoices=summary.invoices,personalReturn=moneyWeightedReturn(state,periodStartDate(state,period)),openInvoices=invoices.reduce((n,i)=>n+i.open,0);
 return <>
  <Stats summary={summary} display={display}/>
  <section className="finance-layout spaced">
   <Card title="Contas e saldo disponível"><div className="finance-list">{finance.accounts.map(a=><div className="finance-row" key={a.id}><span className="finance-row-icon"><Wallet size={17}/></span><span><strong>{a.name}</strong><small>{a.type==='checking'?'Conta corrente':a.type==='savings'?'Poupança':'Dinheiro'}</small></span><strong>{display(balances[a.id])}</strong></div>)}</div><button className="secondary full" onClick={()=>setShowAccount(!showAccount)}><Plus size={16}/>Adicionar conta</button>{showAccount&&<form className="inline-form spaced" onSubmit={addAccount}><label>Nome<input name="name" required maxLength="80" placeholder="Ex.: Santander conta corrente"/></label><div className="form-grid"><label>Tipo<select name="type"><option value="checking">Conta corrente</option><option value="savings">Poupança</option><option value="cash">Dinheiro / carteira</option></select></label><label>Saldo inicial<input name="openingBalance" type="number" step=".01" defaultValue="0"/></label></div><label>Data do saldo inicial<input name="openingDate" type="date" min={state.startDate} max={dateISO()} defaultValue={dateISO()}/></label><button className="primary" disabled={busy}>Salvar conta</button></form>}</Card>
   <Card title="Cartões e faturas" icon={CreditCard}><div className="finance-list">{invoices.filter(i=>i.open>0).slice(0,6).map(i=><div className="finance-row" key={`${i.cardId}-${i.invoiceMonth}`}><span className="finance-row-icon"><CreditCard size={17}/></span><span><strong>{i.cardName} · {i.invoiceMonth}</strong><small>Vence {fmtDate(i.dueDate)} · pago {display(i.paid)}</small></span><strong>{display(i.open)}</strong></div>)}</div>{finance.cards.length===0&&<p className="muted small">Cadastre um cartão para acompanhar compras parceladas e vencimentos.</p>}<button className="secondary full" onClick={()=>setShowCard(!showCard)}><Plus size={16}/>Adicionar cartão</button>{showCard&&<form className="inline-form spaced" onSubmit={addCard}><label>Nome do cartão<input name="name" required maxLength="80" placeholder="Ex.: Santander Visa"/></label><label>Conta usada para pagar<select name="accountId" required>{finance.accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><div className="form-grid"><label>Dia de fechamento<input name="closeDay" type="number" min="1" max="28" defaultValue="10" required/></label><label>Dia de vencimento<input name="dueDay" type="number" min="1" max="28" defaultValue="20" required/></label></div><button className="primary" disabled={busy||!finance.accounts.length}>Salvar cartão</button></form>}</Card>
  </section>
  <section className="two-cols spaced">
   <Card title="Registrar movimentação" icon={ReceiptText}><form onSubmit={addTransaction}><label>Tipo<select name="type" value={type} onChange={e=>setType(e.target.value)}><option value="expense">Despesa à vista</option><option value="income">Receita</option><option value="card_purchase">Compra no cartão</option><option value="card_payment">Pagamento de fatura</option><option value="transfer">Transferência entre contas</option><option value="investment">Aporte em investimento</option></select></label><div className="form-grid"><label>Data<input name="date" type="date" defaultValue={dateISO()} min={state.startDate} max={dateISO()} required/></label><label>Valor total (R$)<input name="amount" type="number" min=".01" step=".01" required/></label></div><label>Descrição<input name="description" required maxLength="160" placeholder="Ex.: Supermercado"/></label>{['income','expense','transfer','investment'].includes(type)&&<label>{type==='transfer'?'Conta de origem':'Conta'}<select name="accountId" required>{finance.accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label>}{type==='transfer'&&<label>Conta de destino<select name="toAccountId" required>{finance.accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label>}{type==='investment'&&<label>Investimento de destino<select name="assetId" required>{state.assets.filter(a=>a.id!=='cash').map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label>}{['expense','income','card_purchase'].includes(type)&&<label>Categoria<select name="category">{categories.map(c=><option key={c} value={c}>{c}</option>)}</select></label>}{type==='card_purchase'&&<><label>Cartão<select name="cardId" required>{finance.cards.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Parcelas<input name="installments" type="number" min="1" max="48" defaultValue="1" required/></label></>}{type==='card_payment'&&<><label>Fatura<select name="invoiceKey" required>{invoices.filter(i=>i.open>0).map(i=><option key={`${i.cardId}-${i.invoiceMonth}`} value={`${i.cardId}|${i.invoiceMonth}`}>{i.cardName} · {i.invoiceMonth} · em aberto {money(i.open)}</option>)}</select></label></>}{['expense','income'].includes(type)&&<label className="checkbox"><input type="checkbox" name="recurring"/>Repetir mensalmente</label>}<p className="form-hint">{type==='card_payment'?'O pagamento reduz o saldo da conta e da fatura, sem duplicar a despesa.':type==='transfer'?'Transferências entre contas próprias não são receita nem despesa.':type==='investment'?'Aporte reduz o saldo da conta e registra a entrada no investimento sem alterar o patrimônio conjunto.':type==='card_purchase'?'A compra entra no orçamento na data da compra; parcelas aparecem nas faturas correspondentes.':'Lançamentos de caixa entram no orçamento e no fluxo do mês.'}</p><button className="primary full" disabled={busy||!finance.accounts.length||(type==='card_purchase'&&!finance.cards.length)||(type==='card_payment'&&!invoices.some(i=>i.open>0))}>Salvar lançamento</button></form></Card>
   <Card title="Orçamento do mês" icon={CalendarDays}><form onSubmit={saveBudget}>{categories.map(c=>{const spent=summary.expensesByCategory[c]||0,limit=Number(budgetValues[c]||0);return <label className="budget-row" key={c}><span>{c}<small>{display(spent)} gasto{limit>0?` de ${display(limit)}`:''}</small></span><input type="number" min="0" step=".01" value={budgetValues[c]??''} placeholder="Sem limite" onChange={e=>setBudgetValues({...budgetValues,[c]:e.target.value})}/>{limit>0&&<i style={{'--budget-progress':`${Math.min(100,spent/limit*100)}%`}}/>}</label>})}<button className="primary full" disabled={busy}>Salvar orçamento</button></form><form className="inline-form spaced" onSubmit={addCategory}><label>Nova categoria<input name="category" maxLength="60" placeholder="Ex.: Pets" required/></label><button className="secondary" disabled={busy}>Adicionar categoria</button></form></Card>
  </section>
  <Card title="Importar extrato Santander (CSV)" icon={Upload}><p className="muted">O arquivo fica neste dispositivo. Revise as linhas e duplicados antes de importar; não conectamos à sua conta nem guardamos senhas.</p><input ref={csvRef} hidden type="file" accept=".csv,text/csv" onChange={loadCsv}/><div className="button-row"><button className="secondary" onClick={()=>csvRef.current?.click()}><Upload size={16}/>Selecionar arquivo CSV</button><button className="secondary" onClick={exportFinanceCsv}><Download size={16}/>Exportar controle</button>{csvRows&&<label>Conta<select value={csvAccount} onChange={e=>setCsvAccount(e.target.value)}>{finance.accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label>}</div>{csvRows&&<><div className="toolbar"><span>{csvRows.filter(r=>r.selected&&!r.duplicate).length} selecionadas · {csvRows.filter(r=>r.duplicate).length} duplicadas · {csvRows.filter(r=>!r.valid).length} inválidas</span><label>Categoria padrão das despesas<select value={csvCategory} onChange={e=>setCsvCategory(e.target.value)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><div className="table-wrap"><table><thead><tr><th>Importar</th><th>Data</th><th>Descrição</th><th>Tipo</th><th>Valor</th><th>Status</th></tr></thead><tbody>{csvRows.slice(0,1000).map((r,i)=><tr key={`${r.row}-${i}`}><td><input type="checkbox" checked={r.selected} disabled={!r.valid||r.duplicate} onChange={e=>setCsvRows(csvRows.map((x,j)=>j===i?{...x,selected:e.target.checked}:x))}/></td><td>{r.date}</td><td>{r.description||'—'}</td><td>{r.type==='income'?'Receita':r.type==='review'?'Revisar transferência / cartão':'Despesa'}</td><td>{r.valid?display(r.amount):'—'}</td><td>{r.possibleTransfer?'Lance manualmente para evitar duplicidade':r.duplicate?'Duplicada':!r.valid?'Inválida':'Nova'}</td></tr>)}</tbody></table>{csvRows.length>1000&&<p className="muted">Mostrando as primeiras 1.000 linhas.</p>}</div><div className="button-row spaced"><button className="primary" onClick={importCsv} disabled={busy||!csvAccount}>Importar selecionadas</button><button className="secondary" onClick={()=>setCsvRows(null)}>Cancelar</button></div></>}</Card>
  <Card title="Movimentações recentes" icon={ReceiptText}><div className="table-wrap"><table><thead><tr><th>Data</th><th>Tipo</th><th>Descrição</th><th>Categoria / cartão</th><th>Valor</th><th></th></tr></thead><tbody>{[...finance.transactions].sort((a,b)=>b.date.localeCompare(a.date)).slice(0,200).map(t=><tr key={t.id}><td>{fmtDate(t.date)}</td><td><span className="tag neutral">{{income:'Receita',expense:'Despesa',transfer:'Transferência',investment:'Aporte',card_purchase:'Compra no cartão',card_payment:'Pagamento de fatura'}[t.type]}</span>{t.installments>1&&<small className="block">{t.installments} parcelas</small>}</td><td>{t.description}</td><td>{t.category||finance.cards.find(c=>c.id===t.cardId)?.name||'—'}{t.type==='card_purchase'&&<small className="block">Fatura {t.invoiceMonth}</small>}</td><td><strong>{display(t.amount)}</strong></td><td><button className="icon-button" aria-label={`Excluir ${t.description}`} onClick={()=>deleteTransaction(t)}><Trash2 size={16}/></button></td></tr>)}</tbody></table>{!finance.transactions.length&&<p className="muted">Suas receitas, despesas e transferências aparecerão aqui.</p>}</div></Card>
  <Card title="Lançamentos recorrentes" icon={Repeat}><p className="muted">Cadastre uma receita ou despesa como recorrente no formulário. Gere cada mês uma vez para evitar duplicidade.</p><div className="finance-list">{finance.recurring.map(r=><div className="finance-row" key={r.id}><span className="finance-row-icon">{r.type==='income'?<ArrowDownLeft size={17}/>:<ArrowUpRight size={17}/>}</span><span><strong>{r.description}</strong><small>{r.day} de cada mês · {r.category}</small></span><strong>{display(r.amount)}</strong></div>)}</div>{finance.recurring.length>0&&<button className="secondary" onClick={addRecurring} disabled={busy}><Check size={16}/>Gerar recorrentes de {month}</button>}</Card>
  <Card title="Comparação de rendimento" icon={ArrowUpRight}><p className="muted">Retorno pessoal considera o momento dos aportes e retiradas. Dividendos ficam no valor da carteira. O período começa no primeiro dia disponível no histórico.</p><div className="button-row"><label>Período<select value={period} onChange={e=>runBenchmarks(e.target.value)}><option value="1">1 mês</option><option value="12">12 meses</option><option value="all">Desde o início</option></select></label>{!benchmark&&<button className="secondary" onClick={()=>runBenchmarks(period)} disabled={benchmarkLoading}>Carregar referências</button>}</div>{benchmarkLoading&&<p className="muted">Carregando séries oficiais…</p>}{benchmark?.error&&<p className="notice danger">{benchmark.error}</p>}{benchmark&&<div className="benchmark-grid"><div><span>Sua carteira · retorno pessoal</span><strong>{personalReturn?`${(personalReturn.total*100).toLocaleString('pt-BR',{maximumFractionDigits:2})}%`:'Histórico insuficiente'}</strong></div>{benchmark.results?.map(r=><div key={r.id}><span>{r.label}</span><strong>{r.return==null?'Sem dados':`${(r.return*100).toLocaleString('pt-BR',{maximumFractionDigits:2})}%`}</strong><small>{r.updatedAt?`Atualizado ${fmtDate(r.updatedAt)}`:r.error||''}</small></div>)}</div>}<p className="source-note">CDI e IPCA: séries públicas do Banco Central (SGS). Ibovespa: estatísticas públicas da B3. Dados de índices não incluem impostos, custos ou dividendos reinvestidos.</p></Card>
  </>
}
